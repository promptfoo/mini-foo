{
  "name": "mini-foo",
  "version": "0.0.1",
  "description": "mini foo interview",
  "main": "index.js",
  "scripts": {
    "postinstall": "cd server && npm install && cd ../app && npm install",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"npm run start --prefix server\" \"npm run start --prefix app\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
