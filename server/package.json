{
  "name": "mini-foo-server",
  "version": "0.1.0",
  "description": "Backend API for evaluation results viewer",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "test": "node --no-warnings=ExperimentalWarning --import tsx --test src/**/*.test.ts",
    "bootstrap": "node --no-warnings=ExperimentalWarning --import tsx bootstrap.ts",
    "start": "node --no-warnings=ExperimentalWarning --import tsx bootstrap.ts && nodemon",
    "build": "tsc --noEmit"
  },
  "author": "Promptfoo Team",
  "license": "MIT",
  "keywords": [
    "api",
    "express",
    "sqlite",
    "typescript"
  ],
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^24.10.1",
    "nodemon": "^3.1.11",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=24"
  },
  "nodemonConfig": {
    "watch": [
      "src",
      "bootstrap.ts",
      "sample_data.ts"
    ],
    "ext": "ts,js,json",
    "ignore": [
      "node_modules",
      "dist",
      "*.test.ts",
      "db.sqlite"
    ],
    "exec": "node --no-warnings=ExperimentalWarning --import tsx src/index.ts",
    "env": {
      "NODE_ENV": "development"
    }
  }
}
